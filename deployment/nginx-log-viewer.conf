server {
    listen 8030;
    server_name sirt-lab-12.internal.example.com;

    root /home/vinay/pub/IR/deployment;
    index log-viewer-index.html;

    # Basic auth
    auth_basic "IR Logs";
    auth_basic_user_file /home/vinay/pub/IR/.htpasswd;

    location / {
        try_files $uri $uri/ =404;
    }
}
