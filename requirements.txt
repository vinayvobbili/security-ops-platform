python-dotenv
matplotlib
# Core Data Processing
pandas>=2.3.2
numpy>=2.3.3
pytz
python-dateutil

# Webex/Communication
webexpythonsdk==2.0.4 # pinned to 2.0.4 for compatibility with webex_bot 1.0.8
webexteamssdk # deprecated
webex_bot==1.0.8 # the framework handles the WebSocket connection setup

# Web Frameworks
Flask
Flask-Limiter
waitress
altair

# Microsoft Teams Bot Framework
botbuilder-core
botbuilder-schema
botframework-connector

# HTTP/Network
requests
aiohttp
urllib3
websockets>=11.0.3,<15.0  # webex_bot 1.0.8 requires 11.0.3, but works with 11.0.3-14.2, v15+ has breaking async changes (requires enhanced_websocket_client.py compatibility patch for 12.0+). Install 14.2 specifically after installing requirements.
aiohttp-retry

# Security/Authentication - Always use latest for security
PyJWT
cryptography
certifi==2024.12.14  # pinned: corp CA cert appended to cacert.pem

# Azure/Cloud
azure-core
azure-devops
msrest

# Data Processing/Analysis
openpyxl
pymssql  # MSSQL client (FreeTDS-backed) — NTLM auth path to EAI DBs (V_APP_INFO etc.)
tabulate
beautifulsoup4
pyarrow
python-docx
unstructured
networkx
msoffcrypto-tool

# Image Processing
pillow
opencv-python-headless
pytesseract
Cartopy

# Security Tools
crowdstrike-falconpy
py7zr  # 7-zip decompression for CrowdStrike RTR file downloads
demisto-py>=3.2.21  # Official Palo Alto XSOAR Python SDK
dnspython  # DNS toolkit (MX/NS/extended DNS lookups for the lookalike engine)
python-whois  # WHOIS lookup library
# Lookalike-domain engine — discover/monitor/score/cluster the typo-squat space
# (own OSS package, extracted from the domain-monitoring portal)
domainflow[ct,whois,score]>=0.1.0

# Task Scheduling/Utilities
schedule
rich
fasteners
tenacity
filelock
config

# AI/LangChain - Use latest for features and fixes
langchain>=1.1.3
langchain-community>=0.4.1
langchain-core>=1.1.3
langchain-openai>=0.3.0
langgraph>=0.2.0  # LangGraph for multi-step workflows
langsmith
rank_bm25
uuid-utils>=0.12.0  # Required by langchain-core 1.1.3+

# Local RAG Components
sentence-transformers>=5.1.0
chromadb>=1.0.21
pypdf>=6.0.0

# Development/Testing
pytest
types-pytz
jsonschema
json-repair
pydantic
pydantic-settings
redis>=5.0
typing_extensions

texttable
tldextract

python-pptx
qrcode[pil]

# Web Search
ddgs

# MCP Server
fastmcp>=2.0

# Detection-as-Code workbench — vendor-neutral lint/draft/overlap/review core
# (own OSS package, extracted from services/detection_pipeline.py)
detflow>=0.1.0

# SOC-in-a-Box kernel — vendor-neutral event contract + bus + case-memory +
# agent framework (own OSS package, extracted from src/components/soc_in_box/).
# The [agent] extra brings the role agents' langgraph/langchain-core; the
# [redis] extra brings the durable RedisBus the agent pipeline runs on.
aisoc[agent,redis]>=0.1.0

# Questionnaire / attestation RAG kernel — backs the Third-Party Risk Assessment
# control evaluation. The handler injects a local LLM + embeddings as its seams.
attestq>=0.2.0

# Refute-first two-LLM code vulnerability scanner — the Code Security feature
# delegates its scan engine to this package through its provider seam.
refutescan>=0.1.0

# Training-engine kernel — vendor-neutral question-bank generate / mixed-format
# sample / deterministic + LLM grade. Powers the lessons feature (quiz grading,
# bank generation, time-limit/anti-cheat policy) through thin adapters that
# inject the local LLM.
quizforge>=0.3.0

# Narrated-slide-reel → MP4 engine — vendor-neutral screenshot / TTS / ffmpeg
# stitch with a master post-step (fades, end-hold, music bed). The lesson and
# how-to video generators are thin seams that supply branded slide HTML + a
# local Kokoro voice.
slidecast>=0.2.1